import React from "react";

export function Title({ text }) {
  return <h1 className="Title heading-primary">{text}</h1>;
}
export function subtitle({ text }) {
  return <h1 className="Title heading-primary">{text}</h1>;
}
